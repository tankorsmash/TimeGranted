$(function(){
  /*
  chrome.runtime.onMessage.addListener(function(request, sender, sendResponse) {
    console.log('messaggegee');
    if (request.message == "time-granted")
      console.log('got it');
      showPopup();
      sendResponse({message: "got-it"});
  });
  */

});